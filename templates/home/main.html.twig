{% extends 'base.html.twig' %}
{% block title %}Main
{% endblock %}

{% block body %}


    {# 
            <br>
            {% for theme in themes %}
            
            
            
            
                {% for word in theme.getWords() %}
                      <a href="">{{ word.text }}</a>
                      <a href="">{{ word.getTranslation().text }}</a>
                {% endfor %}
            {% endfor %}
            <br> #}


    <div class="row m-1">

        <div id="accordion" class="col-md-4">

            {% for theme in themes %}
                <div class="card">
                    <div class="card-header p-0" id="heading{{theme.id}}">
                        <h5 class="mb-0">
                            <button
                                class="btn btn-link" data-toggle="collapse" data-target="#{{theme.id}}" aria-expanded="true" aria-controls="{{theme.id}}">
                                {# <button class="btn btn-link" data-toggle="collapse" data-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne"> #}
                                {{theme.description}}
                                <i class="fas fa-sort-down ml-5"></i>
                            </button>
                        </h5>
                    </div>
                    <div id="{{theme.id}}" class="collapse show" aria-labelledby="heading{{theme.id}}" data-parent="#accordion">
                        <div class="card-body">
                            {% for word in theme.getWords() %}
                                <ul>
                                    <li>
                                        <span>{{word.text}}</span>
                                        ->
                                        <span>{{word.getTranslation().text }}</span>
                                    </li>
                                </ul>
                            {% endfor %}
                        </div>
                    </div>
                </div>
            {% endfor %}


            {# <div class="card">
                                        <div class="card-header p-0" id="headingTwo">
                                          <h5 class="mb-0">
                                            <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                                              Collapsible Group Item #2
                                            <i class="fas fa-sort-down ml-5"></i>
                                            </button>
                                          </h5>
                                        </div>
                                        <div id="collapseTwo" class="collapse" aria-labelledby="headingTwo" data-parent="#accordion">
                                          <div class="card-body">
                                          String -> Chaîne de caractères
                                          </div>
                                        </div>
                                      </div>
                                    
                                      <div class="card">
                                        <div class="card-header p-0" id="headingThree">
                                          <h5 class="mb-0">
                                            <button class="btn btn-link collapsed" data-toggle="collapse" data-target="#collapseThree" aria-expanded="false" aria-controls="collapseThree">
                                              Collapsible Group Item #3
                                             <i class="fas fa-sort-down ml-5"></i>
                                            </button>
                                          </h5>
                                        </div>
                                        <div id="collapseThree" class="collapse" aria-labelledby="headingThree" data-parent="#accordion">
                                          <div class="card-body">
                                          String -> Chaîne de caractères
                                          </div>
                                        </div>
                                      </div> #}


        </div>
    </div>


    <div class="container" align="center">

            {{ form_start(form1) }}
            {{ form_start(form2) }}
            {{ form_start(form3) }}


            <div class="row justify-content-center">
            {{ form_widget(form3.Description, {'attr': {'placeholder': "Type theme name", 'class':"rounded form-control col-sm-3 m-3"}}) }}
            {# <span class="align-middle col-sm-12 m-3" >Or choose existing theme ! </span> #}
            {# <ul>
                  {% for theme in themes %}
                  <li>{{theme.description}}</li>
                  {% endfor %}
            </ul> #}
            {# <select class="form-control col-sm-3">
              <option>Science</option>
              <option>Big Big Big Big Big Big NAME OF THEME</option>
              <option>I Love Pizzas</option>
              <option>Marketing</option>
              <option>IT</option>
            </select>  #}

            </div>


            <div class="row justify-content-center">
                    <img src="assets/countries/france.svg" width="30px" alt="flag" class="align-middle">
                {{ form_widget(form1.Text, {'attr': {'placeholder': "Type Word To Translate", 'class':"rounded form-control col-sm-3 m-3"}}) }}


                <i class="fas fa-arrows-alt-h fa-3x p-3" style="vertical-align: middle;"></i>

                {{ form_widget(form2.Tr_text, {'attr': {'placeholder': "Type Translation", 'class':"rounded form-control col-sm-3 m-3"}}) }}
                    <img src="assets/countries/united-kingdom.svg" width="30px" alt="flag" class="align-middle">


            </div>


            <div class="text-center">
            <button type="submit"  class="btn btn-success btn-red center-block m-2 rounded">Add to Theme ! </button>
            </div>
            {{ form_end(form1) }}
                        {{ form_end(form2) }}

            {{ form_end(form3) }}

    </div>

    {# LIOR CHAMLA 39:08 :) #}

{% endblock %}
